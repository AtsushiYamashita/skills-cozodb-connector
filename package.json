{
  "name": "skills-cozodb-connector",
  "version": "1.0.0",
  "description": "CozoDB integration skill for AI agents",
  "private": true,
  "scripts": {
    "test": "node --test scripts/__tests__/*.test.js",
    "lint": "eslint scripts/",
    "setup": "npm ci && npm run lint && npm test",
    "release": "standard-version"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.0.0",
    "standard-version": "9.5.0"
  },
  "license": "MIT"
}
